# CLAUDE.md

## Rule

1. 모든 주석은 한글로 작성
2. 오버 엔지니어링 금지, 당장 필요한 코드만 작성
3. 단순하고 간결하고 어렵지 않은 가독성 좋은 코드로 만든다.
4. 굳이 없어도 되는 기능은 과감하게 뺀다.
5. 목표를 이루는데 반드시 필요한 기능만 구현한다. 코드 수를 줄인다.
6. 쉽게 이해할 수 있는 코드를 구현한다.
7. 기존 프로젝트의 구현된 부분을 최대한 그대로 유지하면서 새로운 기능만 추가한다.
8. 방대한 코드와 복잡한 로직으로 너무 잘 만들 필요 없다. 10줄보다 1줄이 더 낫다. 기능만 동작하면 된다.

## 유저 시나리오

1. 사용자가 챗봇 화면에 증상을 입력한다.
2. 시스템은 DB에 저장된 증상-질환 데이터를 임베딩 유사도 기반으로 검색한다.
3. 가장 유사한 질환명을 반환하고, 관련 진료과를 함께 추천한다.
4. 사용자 위치 정보 받아옴
5. 인근 병원 상세 정보를 확인
6. 지도를 보여준다.

단일 증상일 때
유저가 “두통이 심해요”라 입력
벡터조회 우선, 일반조회 보조

내과, 신경과 추천 → 현재 위치 반경 2km 병원 리스트

다중 증상, 일치하는 단일 과가 있을때
“속이 쓰리고 신물이 올라와”

결과: 소화기내과 1순위, 내과 2순위(대안 표시)

예외 상황
사용자가 증상이 아닌 질문을 한다
답변이 마음에 들지 않아 재질문을 한다 - (출력값 확인 후 재조정)

💡 기대효과
사용자는 간단한 대화로 자신의 증상에 맞는 진료과와 질환 가능성을 빠르게 확인할 수 있음.

병원 방문 전 1차 자가 진단이 가능하여 불필요한 진료를 줄임.

AI 기반 맞춤형 건강 정보 제공으로 의료 접근성 향상.

## 유저 스토리

1. 증상을 문장으로 입력
2. 이 유저 질문을 갖고 DB 로 보냄
   2-1. 일반 조회
   2-2. 벡터 조회
3. 병원 / 과 추천
   위치 권한 허용 + 바로 길 찾기

ex)
입력 : "허리가 아프고 다리 저림"
결과 : 정형외과 / 신경외과 후보 -> 지도 핀 -> 길 찾기 링크

## 복합 의도, 모호성

1. 다중 증상에 다중 과 후보

- “가슴 답답 + 어지러움”
- 심장내과/호흡기내과/신경과 후보 랭킹 + 설명 배지(근거 문구)

2. 상충되는 단서

- “열은 있는데 한기 없음”
- 특징 중요도 재가중치, 신뢰도 하락 시 “추가 질문” 추천(선택)

## 입력 다양성(언어/형식/오류)

1. 타이포/오탈자

- “배가 아파요 → 배갸 아파요”
- 벡터 유사도 정상 작동, 일반조회는 약화 → 스펠링 교정/제안 배너

2. 구어/속어/이모지

- “속 쓰림 🥲”, “메스꺼움”
- 벡터조회로 의도 인식, 이모지 제거 전처리

3. 부정/제외 조건 포함

- “열은 없는데 기침만 나요”
- 부정 키워드 파싱(“없다”)로 가중치 조정

4. 시간 조건

- “어제부터 복통”, “식후에만 아파요”
- 시간/상황 토큰화 → 과 매칭 보조 특징

5. 연령/임신/소아 맥락

- “임신 20주인데 복통”, “6살 아이가 귀 아파해요”
- 산부인과/소아청소년과 우선 규칙

6. 외국어/코드스위칭

- “migraine 같은 두통”, “SOB(숨가쁨)”
- 의료 약어 사전 + 벡터 라우팅

## 위치, 지도 관련

1. 위치 권한 거부
   지도는 기본 중심 좌표(도심)로, 지역 선택 드롭 다운 제공

2. 커버리지 희소 지역
   반경 2km 내 0개 → 5/10km 확장 안내 + 대중교통/거리 시간 정렬 옵션

3. 영업시간 필터

- “지금 진료하는 곳”
- 요일/시간대 ‘영업 중’ 필터, 야간/휴일 진료 태그

4. 주소/지번·도로명 혼용
   주소 정규화, 클릭 시 지도 앱 연동

## 조회 경로(일반 vs 벡터) 특이 케이스

1. 키워드가 강한 명사형(일반 조회 유리)

- “치질”, “무지 외 반증”
- 일반 조회 1순위, 벡터 보조

2. 장문/서술형(벡터 유리)

- “밥 먹고 나면 명치가 타 들어 가는 느낌”
- 벡터 1순위, 일반 보조

3. 희귀 표기/동의어

- “속 쓰림=가슴앓이=위산 역류”
- 벡터 사전+동의어 사전로 리콜 보강

4. 모델 불확실성 높음

- 상위 과 신뢰도 < 임계치
- 과를 2~3개 제시 + 차이 설명 + 추가 질문 CTA

## 추천, 랭킹, 설명

1. 랭킹 기준 전환
   거리 순 / 평점 순 / ‘영업 중’ / ‘예약 가능’ 토글

2. 병원 카드 정보 불완전
   전화/영업시간 누락 시 안전 문구와 대체 액션(전화 검색) 노출

3. 추천 근거 노출
   “이 증상은 일반적으로 ○○과에서 다룹니다(가이드라인 출처)”

4. 보험/진료비 문의 유도(선택)
   관련 정보 직접 제공 어려울 시 병원 안내로 연결

## 장애, 예외

1. 외부 API 장애(Kakao / 병원정보)

- 캐시 데이터 대체, 부분 기능 강등 (지도 없이 리스트만)

2. 서버 타임아웃

- 3초 경과 시 스켈레톤 + 로더, 6초 경과 시 친절한 재시도

3. DB / 인덱스 이슈

- 일반 / 벡터 조회 중 하나 실패 시 다른 경로 폴백

4. 금칙어 / 응급 위험

- "흉통, 차가운 땀, 의식저하"
- 즉시 119 / 응급실 안내 (빨간 경고), 추천 흐름 중단

## 계정, 개인화

1. 비 로그인

- 최근 검색(local 저장), 즐겨 찾기 제한

2. 로그인

- 즐겨 찾기 / 최근 본 병원 서버 저장, 재 방문 추천

3. 개인정보 최소 수집

- 위치 권한 동의 / 철회, 로그 보존 주기 고지

## 개발 일정

1주차

- 주요 목표 : DB에 데이터 적재, LLM 연결
- 산출물 : PRD, 유저 스토리

2주차

- FastAPI 백엔드 + DB 연동

3주차

- AI 매칭모델 + 통합 테스트
- 증상 → 진료과 자동매칭, MVP 완성

## 기술 스택

Frontend : streamlit
Backend : FastAPI
DB : PostgreSQL
API 연동 : Kakao Map API
AI 모델 : LangChain + OpenAI GPT / Gemini
배포 환경 : Oracle Cloud / Vultr / Docker

## 주요 기능

1. 증상 입력/검색

- 설명 : 사용자가 증상(예: "두통", "복통") 입력
- 기술스택/연계 : OpenAI / LangChain, FastAPI

2. 진료과 매칭

- 설명 : 입력된 증상을 기반으로 적절한 진료과 예측
- 기술스택/연계 : LLM 기반 증상-진료과 매핑 모델

3. 병원 위치 추천

- 설명 : 진료과에 맞는 병원 검색 후 거리순/평가순 추천
- 기술스택/연계 : Kakao Map API / Naver Map API

4. 지도 시각화

- 설명 : 추천된 병원을 지도에 시각화, 클릭 시 길찾기 연결
- 기술스택/연계 : Kakao 지도 SDK

5. 병원정보 보기

- 설명 : 진료 시간, 위치, 전화번호, 진료과, 리뷰 표시
- 기술스택/연계 : 공공데이터포털/보건복지부 API

## 핵심 가치

복잡한 병원 정보 대신 “나에게 맞는 진료과 + 가까운 병원”을 직관적으로 제공
병원 검색·길찾기를 단일 UX로 통합해, 일반 유저도 쉽게 사용할 수 있는 UX

# 🌈 Healthcare AI Agent

사용자와 대화를 통해 사용자에게 적합한 병원을 추천해주고, 해당 병원의 정보와 위치, 찾아가는 방법 등에 대한 정보를 제공해주는
병원 탐색 All in One AI Service

<br><br>

## 💻 기술 스택

| 구분         | 기술                                                                                                                         | 설명                            |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| Backend      | ![FastAPI](https://img.shields.io/badge/FastAPI-009688?style=flat&logo=fastapi&logoColor=white)                              | 비동기 Python 웹 프레임워크     |
| Frontend     | ![Streamlit](https://img.shields.io/badge/Streamlit-FF4B4B?style=flat&logo=streamlit&logoColor=white)                        | 대화형 데이터 대시보드          |
| AI Framework | ![LangChain](https://img.shields.io/badge/LangChain-ffffff?logo=langchain&logoColor=green)                                   | LLM 기반 응용 개발 프레임워크   |
| Database     | ![PostgreSQL](<https://img.shields.io/badge/PostgreSQL%20(pgvector%2017)-336791?style=flat&logo=postgresql&logoColor=white>) | 벡터 임베딩 및 유사도 검색 지원 |

<br><br>

## 🧠 데이터 및 API 활용

| 구분            | 출처 / API                                       | 활용 내용                               |
| --------------- | ------------------------------------------------ | --------------------------------------- |
| 의료 데이터     | **아주대학교병원**                               | 환자 진료 이력 및 임상 데이터 분석      |
| 의료 데이터     | **서울대학교병원**                               | 의료 통계 및 진단 보조 모델 학습        |
| 공공 데이터     | **보건의료빅데이터개방시스템** (기준일: 2025-09) | 전국 병원 정보 및 진료 건수 기반 데이터 |
| 지도 / 위치 API | **KakaoMap API**                                 | 병원 위치 기반 시각화 및 거리 계산      |

<br><br>

## 📁 프로젝트 구조

```
healthcare_ai_agent/
│
├── documents/                        # 프로젝트 문서
├── backend/                          # FastAPI 백엔드 애플리케이션
│   ├── .env                         # 백엔드 환경변수 (DB, LLM API 키 등)
│   ├── app/
│   │   ├── api/                     # API 레이어 - HTTP 인터페이스
│   │   │   └── v1/                 # API 버전 1
│   │   │       └── endpoints/      # 개별 엔드포인트 모듈
│   │   │
│   │   ├── core/                    # 핵심 비즈니스 로직
│   │   │   ├── agents/             # AI 에이전트 구현 및 오케스트레이션
│   │   │   ├── llm/                # LLM 통합 레이어
│   │   │   └── tools/              # 에이전트 도구 및 유틸리티
│   │   │
│   │   ├── models/                  # Pydantic 모델 (스키마)
│   │   ├── services/                # 비즈니스 로직 서비스
│   │   └── utils/                   # 유틸리티 함수
│   │
│   └── tests/                       # 백엔드 테스트
│
├── frontend/                        # Streamlit 프론트엔드 애플리케이션
│   ├── .env                         # 프론트엔드 환경변수 (Backend API URL, 지도 API 키 등)
│   ├── .streamlit/                 # Streamlit 설정
│   ├── components/                 # 재사용 가능한 UI 컴포넌트
│   ├── pages/                      # 멀티페이지 앱 구조
│   ├── services/                   # 프론트엔드 서비스 레이어
│   └── utils/                      # 프론트엔드 유틸리티
│
├── shared/                          # 백엔드/프론트엔드 공유 코드
└── scripts/                         # 개발 및 유틸리티 스크립트
```

<br><br>

## 🤖 AI 모델 선정

선정 기준: 가장 저렴하면서, 최소한의 성능을 만족시키는 모델

| 역할      | 모델명                 | 입력 토큰 비용 (per 1M) | 출력 토큰 비용 (per 1M) | 비고                                                                                        |
| --------- | ---------------------- | ----------------------- | ----------------------- | ------------------------------------------------------------------------------------------- |
| Agent LLM | Gemini 2.5 Flash-Lite  | **$0.10**               | **$0.40**               | 가장 저렴한 2.5 계열 모델. 고성능 필요 최소화 시 적합 :contentReference[oaicite:5]{index=5} |
| Agent LLM | GPT‑4o mini            | ~$0.15                  | ~$0.60                  | OpenAI 저비용 계열. 텍스트 기반 사용 시 유리 :contentReference[oaicite:7]{index=7}          |
| Agent LLM | Gemini 2.5 Flash       | ~$0.30                  | ~$2.50                  | 성능은 높지만 비용도 증가됨 :contentReference[oaicite:9]{index=9}                           |
| Embedding | text-embedding-3-small | **$0.02**               | ~$0 (사실상 무료)       | 비용 매우 저렴. 대량 임베딩 시 유리 :contentReference[oaicite:10]{index=10}                 |

<br><br>

## 🚀 프로젝트 실행하기

### 1️⃣ 가상환경 생성

```bash
python -m venv .venv
```

### 2️⃣ 가상환경 활성화

```bash
# macOS / Linux
source .venv/bin/activate

# Powershell
.venv\Scripts\Activate.ps1
# CMD
.venv\Scripts\activate.bat
# Gitbash
source .venv/Scripts/activate
```

### 3️⃣ 패키지 설치

```bash
# requirements.txt는 backend, frontend 폴더에 각각 존재함
pip install -r requirements.txt
```

### 4️⃣ 서버 실행

```bash
bash scripts/run_backend.sh
```

<br><br>

## 🧭 개발 규칙

프로젝트의 코드 스타일, 브랜치 전략, 커밋 규칙 등은 아래 문서에서 확인할 수 있습니다.  
➡️ [개발 규칙 바로가기](./documents/개발_규칙.md)

## 프로젝트 개요

🏥 Healthcare AI Agent - 프로젝트 개요
핵심 가치
"증상 입력 → 진료과 추천 → 가까운 병원 찾기"를 한 번에 해결하는 서비스
주요 흐름
사용자 증상 입력
→ AI가 진료과 추천
→ 근처 병원 검색
→ 지도로 보여주기
→ 길찾기 연결

```

### 기술 스택 (심플하게)
- **Backend**: FastAPI (빠른 API 서버)
- **Frontend**: Streamlit (간단한 UI)
- **AI**: LangChain + OpenAI/Gemini (증상 분석)
- **DB**: PostgreSQL (병원 데이터 저장)
- **지도**: Kakao Map API (위치 표시)

### 프로젝트 구조 (핵심만)
```

healthcare_ai_agent/
├── backend/ # API 서버
│ └── app/
│ ├── api/ # 엔드포인트
│ ├── core/ # AI 로직
│ └── services/ # 비즈니스 로직
│
├── frontend/ # 사용자 화면
│ ├── pages/ # 페이지
│ └── components/ # UI 컴포넌트
│
└── scripts/ # 실행 스크립트

```

### 핵심 기능 (5개만)
1. **증상 입력**: "배가 아파요" 같은 자연어 입력
2. **진료과 추천**: AI가 적절한 과 찾아줌
3. **병원 검색**: 가까운 병원 리스트 표시
4. **지도 표시**: 병원 위치 시각화
5. **병원 정보**: 전화번호, 영업시간 등

### 데이터 흐름
```

1. 사용자: "두통이 심해요" 입력
2. AI: 신경과/내과 추천
3. 시스템: 현재 위치 기준 2km 내 병원 검색
4. 화면: 지도 + 병원 리스트 표시
5. 클릭: 길찾기 연동
   특이사항 처리

응급상황: "흉통", "의식저하" → 즉시 119 안내
위치 거부: 지역 선택 드롭다운 제공
API 장애: 캐시 데이터로 대체
모호한 증상: 여러 과 추천 + 설명

현재 상태

✅ 기본 FastAPI 서버 구조 완성
✅ 프로젝트 문서화 완료
⏳ AI 증상 분석 로직 개발 예정
⏳ 병원 DB 연동 예정
⏳ 지도 UI 구현 예정

개발 원칙

간단하게
필요한 것만
읽기 쉽게
동작만 하면 됨
